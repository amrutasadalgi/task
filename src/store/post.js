import axios from "axios"
state={
    data:[]
}
getters={
    getData(state){
        return state.data
    }
}
actions={

}
mutations={
    
}

export default{
    state,
    getters,
    actions,
    mutations
}